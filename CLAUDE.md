# Repository guidance

Read and follow `AGENTS.md` and the applicable component `AGENTS.md`.

When running as a MetaHarness semantic reviser, the MetaHarness revision prompt
owns mutable scope, repository exploration, validation and Git operations.
Preserve all repository architecture invariants, but do not expand scope,
invoke unavailable tools, or independently redesign the approved plan.
